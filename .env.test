# Same Postgres container as .env (docker compose up -d), different database name —
# so e2e tests never touch the data used for local development (including the seed).
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/inventory_test?schema=public"
